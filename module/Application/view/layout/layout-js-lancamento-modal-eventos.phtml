<?php

use Application\Controller\Helper\Constantes;

$versao = '?v=0.5';
if ($this->quantidadeDeEventosNoCiclo > Constantes::QUANTIDADES_DE_EVENTOS_NA_TELA) {
    echo $this
            ->headScript()
            ->prependFile($this->basePath() . '/js/circuito_da_visao/verificar-orientacao-tela-lancamento.js' . $versao);
    echo $this->inlineScript();
}